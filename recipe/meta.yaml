{% set name = "curies" %}
{% set version = "0.7.5" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/curies-{{ version }}.tar.gz
  sha256: 7fdc5fcf357b6a4b13dff46ba4544aeea4f1421c584e026a0985d3915ad5206a

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python >=3.8
    - setuptools
    - wheel
    - pip
  run:
    - python >=3.8
    - pytrie
    - pydantic
    - requests

test:
  imports:
    - curies
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://github.com/cthoyt/curies
  summary: Idiomatic conversion between URIs and compact URIs (CURIEs).
  license: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - isuruf
    - dillonroach
